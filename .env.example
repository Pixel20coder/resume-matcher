# NVIDIA NIM OpenAI-compatible API (free tier). Get a key at https://build.nvidia.com
NVIDIA_API_KEY=
# Optional override; defaults to https://integrate.api.nvidia.com/v1
NVIDIA_BASE_URL=
# Public site URL, used for Open Graph / Twitter card absolute links
NEXT_PUBLIC_SITE_URL=
